module Main where

import Data.Char
import qualified Data.String
import qualified Data.List as List hiding (sort)
import qualified Data.Set as Set (member)
import qualified Data.Ord ()
import Data.Maybe (Maybe(Just))
import Data.Either (Either(..))

main = putStrLn "1"
